# Resume-state (Deutsch Lernen) — для продолжения после чистки чата

Проект: `/Users/pabloseoshkin/Klients/Projects/translate.seoshkin.tools`. Стек: Fastify 4 + React 18/Vite + Zustand + PostgreSQL 16 (raw SQL) + Docker. Пользователь — Павел (Павлик/Павлуша), общение по-русски.

**Деплой:** `git push origin main` → `ssh gcloud-seosite "cd /home/seosite/translate && git pull --ff-only && docker compose -f docker-compose.prod.yml build backend frontend && docker compose -f docker-compose.prod.yml up -d backend frontend"`. Миграции авто-применяются на старте (след. номер после 049). Прод-скрипты: `cat script.mjs | ssh gcloud-seosite 'cd /home/seosite/translate && docker compose -f docker-compose.prod.yml exec -T backend node --input-type=module'` (импорты `/app/src/...`). Прод-БД: `docker compose -f docker-compose.prod.yml exec -T db psql -U german_app -d german_learning`.

**Ключевые константы/правила:**
- 22 канонических темы наборов (см. `CANON_THEMES` в `backend/src/services/claude.js`).
- Картинки (gpt-image-1) — платные: генерацию НЕ вызывать из клиента/ученика (owner-only).
- Мультилокаль обязательна (10 языков), контент на язык ученика.
- Банк слов: одна запись на слово, существительные с артиклем, слово в одной теме; авто-разбор при загрузке — `distributeWordsToSets` (`processor.js`) + `/api/reader/distribute`.

## ✅ Сделано и на проде (этой сессией)
Наборы 115→22 (дедуп банка: −62 дубля, артикли, омографы сохранены); заголовки наборов на 9 локалей; дашборд (наборы сверху → уроки, плоско, новый сверху, клик по заголовку/✏️ раскрывает упражнения, ⭐ закреплённые в самый верх, тумблер скрыть, 📷); камера: фото-предложения (абзац+перевод+список слов с галочками), ➕💬 предложение в разговорник, авто-разбор «🎯 по темам», предложения тетрадки → в упражнения; Читалка: 2-й чекбокс копирования (двуязычный+разговор), swap ⇄, переводы gpt-4o; настройки жёсткий persist (visual + push-напоминания авто-переподписка); «Мои сложные слова» в Профиле; QR регистрации в классе; метка «✓ в словаре» зелёная; dev-сайт закрыт от индекса (noindex+robots).

## ⏳ МОИ задачи (крупное/архитектурное — делаем вместе)
1. **«Класс говорит» v2 + распечатка + отчёт**: словоформы, N повторов на слово, проговорить вслух, лист на печать; учитель разрешает пройти самостоятельно; отчёт «кто/группа на чём завис». ≤10 слов из учебника, в конце урока закрепление.
2. **Продающее предложение для школ** (документ/лендинг): автоматизация урока, распечатки, отчёты по ученикам, родители+дети. «Чтобы не смогли отказаться».
3. **Учителю — сложные слова по каждому ученику** + **аналитика владельца по себе** (один аккаунт: Павел = учитель+ученик+руководитель; `myHardWords` уже есть в `analytics.js`).
4. **Раздел «Бэкапы» в админке** (СЕЙЧАС НЕТ): список бэкапов из `/home/seosite/backups`, кнопки «сделать бэкап» (pg_dump), «восстановить» (ОПАСНО — с подтверждением). Делать аккуратно.
5. **Идею распечаток/отчётов — в документацию учителя.**
6. Мелочи: числа «счётными» картинками (опц.); массовая загрузка урока → сразу в наборы (сейчас старый redistribute).

## 📦 FABLE (параллельно, независимо) — `docs/handoff/fable-tasks.md`
✅ СДЕЛАНО 2026-07-16 (все 4, задеплоено): фильтр «без картинок» в Словаре; замена картинки слова своей (webp + сброс кэша); печать набора на 1 лист A4 (`/print/:lessonId`); экспорт документации в PDF. Детали — IDEAS.md §З.

## 🔮 Позже (крупное)
SEO/SSR для индексации на боевом домене; магазин книг (монетизация: посредничество + загрузка контента).
