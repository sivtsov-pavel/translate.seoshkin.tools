# Промт для Sonnet — последний набор (translate.seoshkin.tools)

Скопируй весь файл как первое сообщение новой сессии Sonnet.

---

Ты — Claude (Sonnet), помогаешь Павлу с PWA изучения языков **translate.seoshkin.tools / deutschlernen.ai**.

## Контекст
- Стек: Fastify 4 (raw SQL) + React 18/Vite + PostgreSQL 16 + Docker Compose.
- Бэклог — **`IDEAS.md`** в корне репо: сверяйся, отмечай `[x]` с датой после каждой задачи.
- Деплой сам: `git push` → `ssh gcloud-seosite` → `cd /home/seosite/translate && git pull` → `docker compose -f docker-compose.prod.yml build frontend backend && up -d`. БД: сервис `db`, юзер `german_app`, база `german_learning`.
- ⚠️ nginx-шлюз `seoshkin_nginx` НЕ трогать через `up` с volume-правками — только `nginx -s reload`.
- **Всё сразу на 10 локалей** (файлы i18n — `frontend/src/i18n/` или `frontend/src/store/i18n.js`, найди структуру `t.*` / `extra.json`). Комментарии — русский, имена — английский. См. `MULTILOCALE.md`.
- Правило проекта: **AiTrainer.jsx не трогать** без явной просьбы.
- Бери задачи по одной, собирай локально (`cd frontend && npx vite build`), деплой пачками, проверяй сайт 200.

---

## 🌐 БЛОК 1. Локализация (ТОП-приоритет — Павел тестирует на не-русских локалях)

1. **Хардкод-строки на дашборде** (`frontend/src/pages/Dashboard.jsx`): кнопки-тумблеры «📅 Только сегодня / 📚 Все доступные», «🗂 Наборы: вкл/выкл»; карточки «Выбери ответ» / «Все карточки»; панель прогресса `ProgressPanel` («📘 Обучение по урокам», «💪 Усиление знаний · слова», «карточек сегодня/всего/дней подряд 🔥»); баннер «📅 Выбери календарь обучения»; бейдж «✓ пройдено». Добавить ключи в i18n и перевести на все 10 языков.
2. **Новые разделы Wiki** — перевести на остальные 9 языков (сейчас часть только RU/EN/DE/UK).
3. **Глубокая локализация новых страниц** Grammar / Love / Crossword / Tutors — кнопки, подписи, фильтры каталога репетиторов (страна/город/формат/аудитория/поиск). Механический перевод, идеально для тебя.
4. **Лендинг** (`frontend/src/pages/Landing.jsx`): благотворительный баннер (D6, «бесплатно для всех, помощь украинцам и иммигрантам») + текст «Установите APK — упражнения работают офлайн» (D7) — сейчас только RU + fallback, перевести на 9 локалей.

## 🎨 БЛОК 2. UI / мелкие фичи

5. **Заголовок урока на дашборде** теряется на белом фоне (карточка урока, буковки со звёздочкой) — сделать читаемым (фон/акцент/кнопка), читаемо при скролле. `Dashboard.jsx` (`LessonCard` уже частично выделен — доработать контраст).
6. **Дозагрузка фото в редактировании урока** — как в «Новый урок»: мультивыбор + 2 кнопки 📷/📁. `LessonList.jsx` (сверься с `NewLesson.jsx`).
7. **Кнопка «История» в Читалке** (`TextReader.jsx`) — клиентски через localStorage, показывает прошлые переводы/разговоры, 0 нагрузки на сервер.
8. **Убрать «зоопарк» старых кнопок урока** (⚙️ пересоздать, 🔤 буквы, диктант, речь) под «…» — теперь всё делает одна кнопка «✨ Обработать всё». `LessonList.jsx`.
9. **Блок «+ создать набор»** на дашборде под играми (сейчас вход в конструктор набора только из Словаря). `Dashboard.jsx`.
10. **Раздел «Уроки»:** кнопки под названием/описанием (сейчас слева, схлопываются на узком экране).
11. **Мобилка:** клавиатура на телефоне сдвигает экран — открывать поле ввода **поверх** приложения (не толкать layout).

## 🐞 БЛОК 3. Баги (воспроизвести → починить)

12. **«Войти как» (impersonate)** — ошибка у супер-админа (`frontend/src/pages/Admin.jsx` loginAs + бэкенд-роут). Воспроизвести, починить.
13. **«Обработать всё» — 6 уроков зависли** с ошибкой (проверить `status='error'` / `'processing'`, `drainPendingLessons`, кнопку «🔁 Повторить ошибочные» / `retry-failed`).
14. **JSON-refusal на обложке** — 1-й урок с пустой обложкой («Sorry/Извините») роняет обработку. Нужен graceful (частично есть `extractFromPhoto` try/catch — проверить полноту) + исключение пустых страниц при загрузке PDF (до создания уроков).
15. **Календари «сквозят» между курсами** — расписание немецкого курса видно и в английском. ПРОВЕРИТЬ: возможно уже пофикшено через `key={id}` у `SchedulePicker` в `CourseView.jsx` (в БД раздельно). Если воспроизводится — починить init.
16. **Диктант из выбранных слов** — ПРОВЕРИТЬ: модалка с галочками + поиск в `LessonList.jsx` (`dictModal`, `word_ids`) — возможно уже готова, просто не отмечена в IDEAS. Если работает — отметь `[x]`; если нет — доделай (генерить диктант ТОЛЬКО из отмеченных слов).

---

## Что уже сделано Опусом (НЕ переделывай) — см. IDEAS.md 2026-07-20/21
- **Строгий дрип** + дрип-aware дашборд «Сегодня» (гейт `playableLessonIds`, `needs_schedule`, замки date/prev/no_schedule).
- **«Сдал зачёт = урок пройден»** + единое определение `LESSON_PASSED_HAVING` (все слова урока отработаны ≥1 раз).
- **Фикс «текущего урока»**: `opened = max(календарь, пройдено+1)` в drip.js/courses.js (пройденные+текущий всегда доступны).
- **Панель прогресса** (2 шкалы + плитки карточек/серия/«✓ пройдено» + авто-обновление без F5).
- **Обязательный выбор календаря**; нумерация курса 2; наборы вне дрипа; глобус-перевод учителю; «🔄 Сбросить достижения»; чекбоксы слов в Словаре; фиксы «30 уроков»/«50 карточек».
- **Время в календаре / таймзоны** (users.timezone + notify_prefs, 4 крона по локальному времени, блок «⏰ Когда напоминать» в Настройках).
- **🔑 Свой ключ OpenAI у учителя** (AES-256-GCM, `secretBox.js`, `openaiClient.js`, снятие гейтов картинок/загрузки).
- **B–F**: дрип в разделе «Уроки»; управление меню из супер-админа; убран лишний счётчик в шапке упражнений; выбор курса → потом расписание; «продолжить с места» в сессии.

## Правила
- Короткий план (3–7 пунктов) → делай → скажи что проверить. **Не удаляй данные из БД без бэкапа и подтверждения.**
- Не лезь в архитектурные задачи Опуса (время/таймзоны, ключ OpenAI, dual-mode аккаунт, merge-потеря слов тетради, чат с живым учителем) — они в отдельных сессиях.
- Ключи API — только на сервере в `.env`, в git/локальный `.env` не писать.
