# Промт-ТЗ Флабе: ревью проекта — код (Claude/Соня) и продукт/UX (Павел)

Ты — независимый ревьюер German/Spanish/English-learning PWA (Fastify + React/Vite + PostgreSQL).
Задача Павла: **проверь не только код (что написали Claude и Соня), но и продукт/UX — дай честную
обратную связь и самому Павлу** (как продакт-адвокат ученика). Пиши по-русски, конкретно, со ссылками
на файлы/строки.

## 💸 Правило денег (строго)
- Ревью — это **чтение кода и продукта, БЕЗ трат OpenAI**. Никаких прогонов генерации/переводов/vision.
- Если для проверки хочется что-то сгенерировать через платный ключ — СТОП, опиши, но не запускай.

## Что недавно сделано (ревьюить в первую очередь)
Смотри `IDEAS.md` (разделы 2026-07-25) и последние коммиты. Ключевое:
- **Упражнение «Склонение»** — `frontend/src/components/Conjugation.jsx`, регистрация в
  `ExerciseSession.jsx`, генерация `backend/scripts/gen-conjugation.mjs` + конъюгатор
  `backend/src/services/germanConjugator.js` (тест `.test.js`). Миграция 018.
- **Веер после упражнений** — `ExerciseSession.jsx` (`betweenFan`, TYPE_SEQ, финиш-веер).
- **Цифры/Алфавит на языке курса** — `frontend/src/pages/Vocabulary.jsx`, `utils/numberWords.js`.
- **Безопасная перегенерация** — `backend/src/services/processor.js` (`regenerateExercisesSafe`),
  учёт токенов в `claude.js` (`usage`/`usageCostUSD`).
- **Фильтр пройденных по языку** — `backend/src/routes/exercises.js` (`/completed-lessons`).
- **Навигация** — упрощение переключателей курса (`Layout.jsx`, `Dashboard.jsx`), «Учу».
- **Тур** — `frontend/src/components/Tour.jsx` (+ шаги про курс/расписание/учителя).

## Часть 1 — Ревью КОДА (для Claude/Сони)
Проверь и выдай список находок (по убыванию важности; для каждой: файл:строка → проблема → как чинить):
1. **Корректность.** Логические баги, крайние случаи. Особое внимание:
   - `germanConjugator.js` — верны ли формы для сильных/неправильных? Есть ли глаголы, где ошибается?
   - `gen-conjugation.mjs` — эвристика «глагол» (строго `-en/-eln/-ern` + стоп-лист). Найди ложные
     срабатывания, которые проскочили (не-глаголы среди сгенерированных `conjugation`), и пропуски
     (реальные глаголы, которые отсеялись).
   - `regenerateExercisesSafe` — правда ли сохраняет прогресс (сопоставление по `word_id|type`)? Нет ли
     потери/дублей упражнений?
   - Веер (`betweenFan`): переходы, крайние случаи (один тип, зачёт, хвосты, последнее упражнение).
2. **Безопасность/данные.** Утечки между пользователями/языками/школами (как был баг в
   `/completed-lessons`). Проверь аналогичные эндпоинты на фильтр `target_lang`/`owner`/`school`.
3. **i18n-покрытие** (это ГЛАВНАЯ жалоба Павла — «локали не работают»). Найди захардкоженные русские
   строки в UI (кроме намеренного контента). Есть отдельный подробный handoff на это —
   `handoff/FABLE_i18n_refactor.md` — можно совместить.
4. **Чистота/долги.** Мёртвый код (напр. неиспользуемые `DE_NUMBERS`/`activeCourse` в Vocabulary/
   Dashboard после рефакторингов), дубли, слишком большие файлы.
5. **Проверь сборку:** `cd frontend && npm run build` и `node backend/src/services/germanConjugator.test.js`.

## Часть 2 — Ревью ПРОДУКТА/UX (для Павла — честно)
Пройди глазами ученика (немецкий И испанский под учеником) и учителя. Дай Павлу честный список:
- Что **непонятно/путает** (навигация, переключение языка/курса, где что лежит).
- Где **много кликов** или лишние сущности (Павел уже убирал дубли переключателей — поищи ещё).
- **Веер и порядок упражнений** — реально ли мотивирует, не раздражает ли частотой; понятен ли
  прогресс/остаток.
- **Склонение** — понятен ли экран, оба режима; не слишком ли сложно для A1.
- **Мультиязычность** — где ещё «протекает» русский/немецкий для не-тех пользователей.
- 3–5 **приоритетных улучшений** продукта (не мелочи), с обоснованием «почему ученику лучше».

## Формат ответа
1. **Код — находки** (список, по важности, файл:строка).
2. **Продукт/UX — для Павла** (список наблюдений + топ-5 улучшений).
3. **Что проверил сам** (сборка, тесты — результат).
Не чини сам в этом заходе (кроме явного запроса Павла) — сначала список, Павел решит приоритеты.
EOF
echo "готов: handoff/FABLE_review_prompt.md"