-- Уроки, автосозданные из bulk-загрузки PDF курса (1 страница = 1 урок). Если после
-- обработки такой урок остался пустым (обложка/пустая страница, ИИ отказался извлечь слова) —
-- прячем его из списков по умолчанию (данные не удаляем, фото/урок остаются в БД).
ALTER TABLE lessons ADD COLUMN IF NOT EXISTS auto_pdf boolean NOT NULL DEFAULT false;
